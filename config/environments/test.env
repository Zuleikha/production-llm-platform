# Committed, NON-SECRET defaults for the `test` profile.
# Loaded automatically when ENVIRONMENT=test (used by the pytest suite).
ENVIRONMENT=test
SERVICE_NAME=api
API_HOST=127.0.0.1
API_PORT=8000
DEBUG=false
LOG_LEVEL=WARNING
LOG_FORMAT=json
