# Committed, NON-SECRET defaults for the `dev` profile.
# Loaded automatically when ENVIRONMENT=dev (the default).
ENVIRONMENT=dev
SERVICE_NAME=api
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true
LOG_LEVEL=DEBUG
LOG_FORMAT=console
