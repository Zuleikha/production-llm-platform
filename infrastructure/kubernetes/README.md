# kubernetes — placeholder

> **Status: planned, not yet implemented.** Delivered in **Stage 7 (kubernetes)**.

Stage 1 runs the whole stack with Docker Compose only. Kubernetes manifests /
Helm charts (Deployments, Services, HPA, probes wired to `/health` and
`/ready`, ConfigMaps/Secrets) are added in Stage 7. Nothing here is applied yet.
