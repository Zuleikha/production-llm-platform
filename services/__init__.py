"""Platform services.

Each subpackage is one deployable/logical service. Only ``api`` carries real
implementation in Stage 1; the rest are interface stubs whose contracts are
filled in during later stages (see each package's README).
"""
